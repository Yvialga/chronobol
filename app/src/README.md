# Création de la base de données
non-créé en db : email (Runner) ? attente de confirmation

# Migration de la base de données
cd répertoire principal -> docker-compose exec frontend bash <!--frontend: service ; bash: command-->

# Dernière tâche

# in brainstorming
- pour page de compétition, j'ai besoin de :
    CRUD de compétition
    avoir un espace de créé - slug dans l'url
    accès de session utilisateur