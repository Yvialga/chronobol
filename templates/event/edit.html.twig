{% extends 'authenticated_user.html.twig' %}

{% block title %}Modifier la compétition{% endblock %}

{% block main %}
    <a href="{{ path('app_event_home') }}" class="underline no:underline text-sm ">Retourner à la liste</a>
    <h1 class="font-semibold text-3xl">Modifier la compétition</h1>

    {{ include('event/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    {{ include('event/_delete_form.html.twig') }}
{% endblock %}
