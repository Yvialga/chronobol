{% extends 'authenticated_user.html.twig' %}

{% block title %}Modifier l'équipe{% endblock %}

{% block main %}
    <section class="m-8">
        <a href="{{ path('app_team_index', {slug: event.slug}) }}" class="underline hover:no-underline">Retour à la liste des équipes</a>
        <h1 class="text-3xl">Modifier l'équipe</h1>

        {{ include('team/_form.html.twig', {'button_label': 'Update'}) }}

        {% if is_granted('ROLE_ADMIN') %}
            {{ include('team/_delete_form.html.twig') }}
        {% endif %}
    </section>
{% endblock %}
